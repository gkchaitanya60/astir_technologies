(()=>{'use strict';const menu=document.querySelector('.menu-toggle'),nav=document.querySelector('#main-navigation');if(!nav)return;const groups=[...document.querySelectorAll('.nav-dropdown')];function close(except){groups.forEach(g=>{if(g!==except){g.querySelector('button').setAttribute('aria-expanded','false');g.querySelector('.dropdown-panel').hidden=true}})}function setMenu(open){nav.classList.toggle('is-open',open);menu.setAttribute('aria-expanded',String(open));menu.querySelector('.sr-only').textContent=open?'Close navigation':'Open navigation';if(!open)close()}menu.addEventListener('click',()=>setMenu(menu.getAttribute('aria-expanded')!=='true'));groups.forEach(g=>{const b=g.querySelector('button'),p=g.querySelector('.dropdown-panel');b.addEventListener('click',()=>{const open=p.hidden;close(g);p.hidden=!open;b.setAttribute('aria-expanded',String(open))});g.addEventListener('focusout',e=>{if(!g.contains(e.relatedTarget)){p.hidden=true;b.setAttribute('aria-expanded','false')}})});document.addEventListener('click',e=>{if(!e.target.closest('.nav-dropdown'))close();if(!e.target.closest('.site-header'))setMenu(false)});document.addEventListener('keydown',e=>{if(e.key!=='Escape')return;const g=groups.find(g=>!g.querySelector('.dropdown-panel').hidden);if(g){close();g.querySelector('button').focus()}else if(nav.classList.contains('is-open')){setMenu(false);menu.focus()}});matchMedia('(min-width:1101px)').addEventListener('change',()=>setMenu(false));})();

(()=>{const header=document.querySelector('.site-header');if(!header)return;let queued=false;const update=()=>{header.classList.toggle('is-scrolled',window.scrollY>8);queued=false;};window.addEventListener('scroll',()=>{if(!queued){queued=true;requestAnimationFrame(update)}},{passive:true});update();})();
